@using WebMvc.Models
@model IEnumerable<WebMvc.Models.FriendViewModel>

@*@{
        var listaPessoas = ViewBag.Friends;
    }*@

<h2>Primeiro Formulario</h2>

@using(Html.BeginForm("Index", "FirstFriend", FormMethod.Post)) {

    <table name="lista" class="table table-condensed table-hover">
        <thead>
            <tr>Lista</tr>
        </thead>
        <tbody>
            @{ int i = 0;
            }
            @foreach(var friend in (IEnumerable<FriendViewModel>)TempData.Peek("Friends")) {
                <tr>
                    <td><input type="checkbox" name="lista[@i].Selected" checked="@friend.Selected" /></td>
                    <td> @friend.Name </td>
                    <td> @friend.LastName </td>
                    <td> @friend.Email </td>
                </tr>
                i++;
            }
        </tbody>
    </table>

    <div class="form-group">
        <input type="submit" value="Próximo" class="btn btn-default" />
    </div>
}


